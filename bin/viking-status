#!/usr/bin/env node

var program = require('commander');
var status = require('../lib/bootstrap/status')

program
  //.usage('viking build [scripts] [styles] [files]')
  .usage('')
  .option('-t, --tail', 'tail the host logs')
 
program.parse(process.argv);

status(program)