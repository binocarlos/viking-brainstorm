#!/usr/bin/env node
var args = require('minimist')(process.argv, {
  boolean:['tail'],
  alias:{
    t:'tail'
  }
})

var core = require('../lib/bootstrap/core')

var cmd = args._[2]

if(cmd=='start'){
  core.start(function(){

  })
}
else if(cmd=='stop'){
  core.stop(function(){

  })
}