#!/usr/bin/env node
var program = require('commander')
var Bootstrap = require('../lib/bootstrap')

program
  .usage('')
  .option('-t, --tail', 'tail after boot')

program.parse(process.argv);

Bootstrap.start(function(err){
	if(program.tail){
		Bootstrap.tail()
	}
})