#!/usr/bin/env node
var program = require('commander')
var Bootstrap = require('../lib/bootstrap')

program
  .usage('')
  .option('-sd, --seed', 'generate a new etcd cluster token')

program.parse(process.argv);

Bootstrap.etcd(program)