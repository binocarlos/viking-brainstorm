#!/usr/bin/env node
var program = require('commander')
var Host = require('../lib/host')

program
  .usage('')

program.parse(process.argv);

// we let etcd get setup before starting the host
setTimeout(function(){
	Host(program || {})	
}, 2000)
