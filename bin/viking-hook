#!/usr/bin/env node

// this is how we can enable plugins

// any significant event will run its data via the hook on stdin
//
// this lets any program manipulate the data
//
// the name of the hook followed by hook specific args on the command line
// the data to process on stdin
//
var args = require('minimist')(process.argv, {
  alias:{
    
  }
})

var name = args._[0]

process.stdin.pipe(process.stdout)