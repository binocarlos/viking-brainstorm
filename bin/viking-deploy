#!/usr/bin/env node
var deploy = require('../lib/bootstrap/deploy')
var args = require('minimist')(process.argv, {
  alias:{
    a:'app',
    c:'config',
    r:'registry'
  },
  default:{
    config:'viking.yml'
  }
})
deploy(args)